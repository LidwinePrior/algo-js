// 3.1 addtionner des éléments 

let arr = [1, 2, 3, 4, 5];
let amount = 0;
for( let i= 0; i < arr.length; i++) {
    amount += arr[i];
} console.log(amount);


let num = [100, 101, 102];
let add = 0;
for(let a= 0; a < num.length; a++) {
    add += num[a];
} console.log(add);